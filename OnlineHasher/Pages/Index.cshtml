@page
@model IndexModel

<form method="get" asp-page="Index">
    <label for="password">Password</label>
    <input type="text" id="password" name="password" value="@Request.Query["password"]" placeholder="Password" required autofocus />
    <br />

    <label for="salt">Salt (optional)</label>
    <input type="text" id="salt" name="salt" value="@Request.Query["salt"]"  placeholder="Salt" />
    <br />

    <input type="submit" value="Generate hash" />
</form>

@if (!string.IsNullOrWhiteSpace(Model.Hash))
{
    <label for="hash">Hash</label>
    <output contenteditable="true" id="hash">@Model.Hash</output>
}